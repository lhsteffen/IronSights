import 'package:flutter/material.dart';

class RoutinepageWidget extends StatelessWidget {

  RoutinepageWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Routine Page")
    );
  }
}